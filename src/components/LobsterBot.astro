---
// Lobster-themed AI assistant component
// Playful animated lobster character
---

interface Props {
  size?: 'small' | 'medium' | 'large';
}

const { size = 'medium' } = Astro.props;

const sizeClasses = {
  small: 'w-16 h-20',
  medium: 'w-24 h-32',
  large: 'w-32 h-48'
};
---

<div class={`inline-flex items-center ${sizeClasses[size]}`}>
  <div class="text-6xl">🦞</div>
</div>
